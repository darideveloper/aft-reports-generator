# Tasks

1. [x] Remove `setGeneralData` calls from `onChange` handlers in `GeneralDataScreen.tsx`. <!-- id: remove-set-general-data -->
2. [x] Verify `handleNext` correctly saves all fields using `setEmail`. <!-- id: verify-handle-next -->
3. [x] Verify validation still works as expected. <!-- id: verify-validation -->
